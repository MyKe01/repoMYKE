#include <stdlib.h> 
#include <algorithm>
#include <string> 
#include <iostream> 
#include <vector> 
using namespace std;

char nome[30];
char cognome[30];
char numero[30];
vector <string> rubrica; 
rubrica.push_back(nome, cognome, numero);
int main(){
	cout 
	for (int i =0; i < rubrica.size(); i++ ){
		cout << rubrica(i);
	}
}

